Script CGI et C
passwd.c : permet d'afficher le tableau des utilisateurs avec leurs répertoires personnels.
varLog.c : permet d'afficher un tableau contenant le temps, l'ouverture ou fermeture d'un session, et l'utilisateur dans le fichier /var/log/auth.log.

NB: Pour le fichier source varLog.c, il faudrait d'abord changer la permission du fichier /var/log/auth.log (640) en 644 pour permettre la lecture du fichier.

